part of 'section_card.dart';

class CrmDivider extends StatelessWidget {
  const CrmDivider({super.key});

  @override
  Widget build(BuildContext context) =>
      const Divider(height: 1, thickness: 1, color: AppColors.slate100);
}
